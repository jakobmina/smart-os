#ifndef BANNER_H
#define BANNER_H

#include "qcore_metriplectic.h"

/**
 * @brief Renders the dynamic SMOPSYS ASCII banner with hologram effects.
 * @param state Pointer to the current system state.
 */
void render_banner(const SystemState *state);

#endif // BANNER_H
